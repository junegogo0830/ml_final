# VALIDATION_LOG.md

프로젝트: 기업 파산 예측 기반 대출 심사 시스템

AI(Claude)가 제안한 내용 중 검증을 거쳐 수정하거나 채택한 사례를 기록한다. 데이터 구축·라벨 정의·모델 설계·평가·시스템 구현 전 단계를 망라한다.

1. 기존 라벨 정의의 오류 발견

AI가 기존 bankruptcy 테이블의 스키마(delist_date, delist_reason, reason_category 컬럼)를 분석하여 이 테이블이 실제로는 "상장폐지(delist) 리스트"이며 자진 상장폐지, 합병 소멸, 코스닥→코스피 이전 상장 등 재무 위기와 무관한 사유가 다수 포함되어 있음을 지적함. 이에 라벨 정의를 "상장폐지 여부"에서 "DART 공시 기반 5종 위기 이벤트(부도발생/회생절차/해산/영업정지/워크아웃) 발생 여부"로 전면 재정의함. 자진 상폐는 재무 건전성과 무관하거나 오히려 우량 기업의 의사결정인 경우가 많아, 이를 파산으로 라벨링하면 모델이 노이즈를 학습하게 되고 발표 시 라벨 타당성에 대한 반박을 받을 수 있다고 판단함.

2. 상장폐지 데이터의 라벨 활용 여부 검증

AI가 03 단계에서 수집한 delisting 4,356건을 처음에는 보조 위기 신호로 라벨에 포함하자고 제안함. 이를 검증하기 위해 report_nm을 noise(절차 안내 공시), voluntary(자진·합병·이전상장), real_delisting(실제 상장폐지 사건), other 4가지로 분류한 후, 각 분류의 기업이 다른 위기 이벤트(부도/회생/해산)와 얼마나 겹치는지 교차검증을 수행함. real_delisting으로 분류된 347개 기업 중 다른 위기 이벤트를 동반한 비율이 33.1%에 불과했으며, 나머지는 자진 상장폐지 사례(한주라이트메탈, 생고뱅코리아홀딩스 등)로 확인됨. 이에 delisting을 독립 라벨 source로 사용하지 않고 "다른 핵심 위기를 동반한 delisting"만 보조 정보(has_delisting 컬럼)로 보존하는 방식으로 수정함.

3. 모델 성능 수치(ROC-AUC 0.9615)에 대한 비판적 검토

AI가 기존 CNN+LSTM 모델의 ROC-AUC 0.9615를 baseline 성능으로 그대로 받아들이지 않고, 양성:음성 1:25의 극단적 불균형 데이터에서 ROC-AUC 0.96 이상이 나오는 것은 데이터 누수의 신호로 해석되어야 한다고 지적함. 이에 라벨 시점과 재무제표 시점의 선후관계, CTGAN 증강 시점, StandardScaler fit 범위, 관계 피처의 시점 정보 등 누수 가능성 항목을 체크리스트로 정리하고 데이터 재구축 후 재검증하기로 결정함. 누수 제거 후 ROC-AUC는 0.7472로 조정되었으며 이를 정직한 baseline으로 채택함.

4. 비상장 기업 수집 범위 결정

AI가 DART 등록 entity 118,179개를 모두 재무제표 수집 대상으로 삼지 말고 "사업보고서 제출 의무가 있고 실제로 제출한 기업"만으로 좁히자고 제안함. 근거로 외감 비상장기업은 재무제표가 아닌 감사보고서만 공시하며 fnlttSinglAcntAll API로는 조회되지 않는다는 점을 제시함. 이를 검증하기 위해 비상장 114,212개 중 일부를 대상으로 list API로 사업보고서 제출 이력을 직접 조회한 결과 690개만 확인됨. 만약 전체를 수집했다면 API 호출의 90% 이상이 무의미했을 것으로 판단되어 학습 대상을 4,657개(상장 3,967 + 비상장 690)로 확정함.

5. 데이터 수집 재시작 로직의 결함 발견

AI가 초기에 작성한 수집 스크립트는 --resume 플래그를 명시해야만 기존 진행 상황을 이어가도록 설계되어 있었음. 실제 실행 로그를 확인한 결과, --resume 없이 재실행 시 이미 수집한 데이터(is_active 마킹)가 0으로 리셋되는 파괴적 버그가 발생함. 이에 resume을 별도 플래그가 아닌 기본 동작으로 변경하고 파괴적 리셋 로직을 제거함. 별도 플래그 방식은 사용자가 깜빡하고 빼먹을 경우 수일치 수집 작업이 무효화되는 치명적 리스크가 있었기 때문에 근본적인 구조 변경이 필요하다고 판단함.

6. API 호출 한도 처리 방식 재검토

AI가 처음에는 일정 횟수(20,000회) 도달 시 60초 대기 후 재시도하는 방식으로 설계함. 그러나 실제 DART API의 일일 한도 초과 응답(status 020)은 분당 제한과 다른 성격이며, 60초 대기로는 한도가 복구되지 않음을 확인함. 이에 020을 별도 예외(DartQuotaExceeded)로 분리하여 즉시 작업을 중단하고 자정(KST) 이후 동일한 명령으로 재실행하면 자동으로 이어서 진행되도록 재시작 로직을 보완함.

7. GraphSAGE 결합 방식 의사결정

AI가 관계 정보를 시계열 모델에 통합하는 두 가지 옵션을 제시함. 옵션 A는 이웃 기업의 재무 지표 평균을 시계열 모델의 피처로 직접 추가하는 방식이고, 옵션 B는 시계열 모델과 GraphSAGE를 독립적으로 학습한 후 임베딩을 결합하는 방식임. 옵션 A는 이전 실험에서 neighbor_count 같은 단순 카운트 피처가 importance 상위에 올라오며 노이즈로 작용해 PR-AUC가 0.05 하락한 이력이 있음을 근거로 옵션 B를 채택함. 또한 GAT 대신 GraphSAGE를 선택한 이유는 본 프로젝트의 엣지 가중치가 사전 정의되어 있어 attention 학습 여지가 적고, 양성 샘플 수(206개)가 제한적이므로 더 단순한 GraphSAGE가 과적합 위험이 낮다고 판단했기 때문임.

8. CTGAN 데이터 증강 폐기 결정

AI가 처음에는 양성 샘플 부족 문제를 해결하기 위해 CTGAN 증강을 제안함. 이를 적용한 결과 양성 159×3 합성 시 PR-AUC가 0.02~0.06 하락하는 결과가 나옴. 원인을 분석한 결과, CTGAN은 충분한 양성 샘플로부터 분포를 학습할 때 효과를 발휘하지만 본 프로젝트의 양성 159개라는 규모에서는 mode collapse가 발생하여 합성 데이터가 실제 분포를 제대로 반영하지 못한다는 결론에 도달함. 합성 양성을 추가할수록 PR-AUC가 일관되게 하락하는 패턴을 5-seed 실험으로 재확인한 후 CTGAN 증강을 폐기함.

9. 평가 지표 선택 검증

AI가 초기에는 Accuracy를 기본 평가 지표로 제안함. 그러나 양성:음성 비율이 1:8.6인 불균형 데이터에서 Accuracy는 다수 클래스(정상)만 예측해도 89% 이상이 나오므로 모델의 실질적 분류 능력을 측정하지 못한다고 판단함. 이에 평가 지표를 ROC-AUC, PR-AUC, Gini, KS 네 가지로 변경하고 5-seed 평균과 표준편차를 함께 보고하도록 수정함. PR-AUC는 양성 클래스 성능을 직접 반영하고, KS는 신용평가 업계에서 표준적으로 사용되는 지표로서 실무 적용 가능성을 평가하는 데 적합함.

10. 시계열 모델 후보 비교 결과 검증

AI가 LSTM, CNN+LSTM, TFT 세 모델을 동일 조건에서 비교하는 실험을 제안함. 각 모델을 5개 시드로 반복 실험한 결과 TFT가 ROC-AUC 0.748, PR-AUC 0.364로 가장 우수하게 나왔으나, 표준편차가 다른 모델보다 크다면 우연일 가능성이 있다고 판단하여 95% 신뢰구간을 추가로 계산함. 검증 결과 TFT의 우월성이 통계적으로 유의했으며, 이를 최종 모델로 확정함. 만약 신뢰구간이 겹쳤다면 더 단순한 LSTM을 선택하는 것이 운영 효율성 측면에서 유리했을 것임.

11. 비즈니스 룰 계수 산정 근거 보강

AI가 신용 한도 산식에서 담보가치를 70%만 반영하는 이유를 처음에는 명확히 제시하지 못함. 추가 조사를 통해 Basel III에서 부동산 담보의 LTV(Loan-to-Value) 상한이 70%로 권장되며, 한국 은행권에서도 이 기준을 따른다는 점을 확인함. 또한 PD 구간별 스프레드(+1.5~5.0%)도 신용등급별 회사채 스프레드 실측값을 참고하여 보정함. 이렇게 비즈니스 룰의 모든 계수에 출처를 명시함으로써 보고서의 신뢰성을 확보함.

12. SHAP 기반 거절 사유 생성의 한계 확인

AI가 SHAP value를 자연어 문장으로 자동 생성하는 로직을 제안함. 그러나 GPT 계열 LLM이 없는 환경에서는 SHAP 값을 의미 있는 자연어로 변환하기 어렵고, 어색한 문장이 생성되어 오히려 신뢰도를 떨어뜨릴 수 있다고 판단함. 이에 자연어 생성 부분은 포기하고 SHAP 상위 피처 3~5개와 그 기여도를 단순 템플릿("debt_ratio가 업종 평균 대비 X% 높음" 형태)으로 표시하도록 변경함. 이는 정확성과 가독성 사이의 타협이며, 향후 LLM 통합 시 자연어 생성 기능을 다시 시도할 계획임.

13. 시계열 길이(SEQ_LEN) 결정

AI가 SEQ_LEN을 5년치로 설정하자고 처음 제안함. 그러나 양성 487건 중 label_date 이전 5년치 재무를 보유한 기업은 약 200개 수준으로 줄어들어 학습 데이터가 과도하게 손실됨을 확인함. 이에 SEQ_LEN을 3년으로 조정한 결과 학습 가능 양성이 261개로 증가했으며, 부족한 시점은 0 패딩과 마스킹으로 처리함. 모델 성능에는 큰 차이가 없었으나 학습 안정성과 양성 표본 확보 측면에서 SEQ_LEN=3이 더 적합하다고 판단함.

14. 시간 분할(Temporal Split) 기준 연도 결정

AI가 처음에는 firm-level stratified 8:2 분할을 제안함. 그러나 파산 예측은 본질적으로 시간 예측 문제이므로, 시간을 무시한 무작위 분할은 미래 정보를 학습에 사용하는 효과를 낼 수 있다고 판단함. 이에 2022년 이전을 학습, 2023년 이후를 평가로 분할하는 temporal split을 도입함. 기준 연도를 2022년으로 정한 이유는 양성 라벨이 시점별로 고르게 분포되어 train/test 양쪽에 충분한 양성이 확보되기 때문임. 만약 더 늦은 시점을 기준으로 하면 test 양성이 너무 적어 평가가 불안정해지고, 더 이른 시점이면 train 양성이 부족해짐.

15. 최종 보고서의 결론 부분 톤 조정

AI가 처음 작성한 결론은 데이터 누수 통제에만 초점이 맞춰져 있었음. 본 프로젝트는 누수 통제만이 아니라 데이터셋 구축, 라벨 재정의, 모델 결합, 비즈니스 룰 통합, 대시보드 배포까지 end-to-end 시스템 구축이 핵심이라는 점을 반영하여 결론을 다시 작성하도록 요청함. 데이터 측면의 기여, 모델 측면의 기여, 시스템 측면의 기여, 그리고 정직성 측면의 기여 순으로 균형 있게 서술하도록 수정함.
